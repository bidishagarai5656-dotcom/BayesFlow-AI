[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/bayesflow run build"
  publish = "artifacts/bayesflow/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
